"""bc_pipeline — the baseball-coaster-data pipeline package.

Fetches raw game pages, parses them into canonical game files under ``games/**``,
and replays parsed games to validate them against the schema oracles. This is the
package skeleton; the fetch/parse/replay logic is implemented in later issues.
"""
